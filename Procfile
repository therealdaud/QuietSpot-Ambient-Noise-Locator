# Render / Heroku deployment — runs the Python FastAPI backend
# Update your Render service's Start Command to match this if needed.
web: cd python-backend && uvicorn main:app --host 0.0.0.0 --port $PORT
